# ============================================================================
# JELLYFIN MEDIA STACK - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
#
# NEVER commit the .env file to version control!
# ============================================================================

# ============================================================================
# USER AND GROUP IDS
# ============================================================================
# Get these values by running: id
# Example output: uid=1000(user) gid=1000(user)
# Use the uid value for PUID and gid value for PGID

PUID=1000
PGID=1000

# ============================================================================
# TIMEZONE
# ============================================================================
# Find your timezone: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# Examples: America/New_York, Europe/London, Asia/Tokyo, America/Los_Angeles

TZ=America/New_York

# ============================================================================
# BASE PATHS
# ============================================================================
# Where Docker will store all configuration files
# This should be a path with sufficient space (50-100GB recommended)

BASE_PATH=/opt/docker/media-stack

# Where your media files are stored (NFS mount, local storage, SMB share, etc.)
# This is where movies, shows, music, books, and comics will be saved
# Ensure this path has LARGE storage capacity (1TB+ recommended)

MEDIA_SHARE=/mnt/media

# Optional: Separate path for transcoding cache (faster if on SSD)
# Leave commented out to use default location inside Docker
# Uncomment and set if you want to use a dedicated fast disk for transcoding

# TRANSCODE_PATH=/transcode_cache

# ============================================================================
# VPN CONFIGURATION (GLUETUN + MULLVAD)
# ============================================================================
# Get these from: https://mullvad.net/en/account/wireguard-config
# 1. Login to your Mullvad account
# 2. Generate a WireGuard configuration
# 3. Download the .conf file
# 4. Extract the values below from the file

# Your WireGuard private key (from [Interface] section)
# Example: wOEI9rqqbDwnN8/Bpp22sVz48T71vJ4fYmFWujulwUU=
WIREGUARD_KEY=YOUR_PRIVATE_KEY_HERE

# Your WireGuard IP address (from Address line, use IPv4 only)
# Example: 10.68.123.45/32
WIREGUARD_ADD=10.0.0.0/32

# Server city/cities (comma-separated for multiple)
# Examples: Amsterdam, Stockholm, New York, Los Angeles
# See available cities: https://mullvad.net/en/servers
VPN_SERVER_CITIES=Amsterdam

# WireGuard MTU (Maximum Transmission Unit)
# Default: 1200 (works for most)
# If VPN is unstable, try: 1280, 1380, or 1420
WIREGUARD_MTU=1200

# Public IP API (to verify VPN connection)
# Options: ipinfo, ip2location (requires token), ipapi, ifconfigco
# Leave as ipinfo unless you have specific needs
PUBLICIP_API=ipinfo

# Public IP API Token (only needed for ip2location)
# Leave empty if using ipinfo
PUBLICIP_TOKEN=

# ============================================================================
# JELLYFIN CONFIGURATION
# ============================================================================
# The public URL where Jellyfin will be accessible
# Examples:
#   - Local: http://192.168.1.100:8096
#   - Domain: https://jellyfin.yourdomain.com
# This is used for DLNA and discovery features

JELLYFIN_URL=http://192.168.1.100:8096

# ============================================================================
# API KEYS FOR *ARR APPS
# ============================================================================
# These will be generated automatically on first startup
# After first run, copy them from each app's Settings -> General -> API Key
# Then paste them here for cross-app communication

# Sonarr API Key (Settings -> General -> Security -> API Key)
SONARR_KEY=REPLACE_AFTER_FIRST_STARTUP

# Radarr API Key (Settings -> General -> Security -> API Key)
RADARR_KEY=REPLACE_AFTER_FIRST_STARTUP

# Lidarr API Key (Settings -> General -> Security -> API Key)
LIDARR_KEY=REPLACE_AFTER_FIRST_STARTUP

# Readarr API Key (Settings -> General -> Security -> API Key)
READARR_KEY=REPLACE_AFTER_FIRST_STARTUP

# ============================================================================
# QBITTORRENT CREDENTIALS
# ============================================================================
# Set your qBittorrent WebUI credentials
# Default username is 'admin' - you can keep it or change it
# IMPORTANT: Change the default password!

QBIT_USER=admin
QBIT_PASS=your_secure_password_here

# ============================================================================
# GLUETUN NETWORK CONFIGURATION
# ============================================================================
# Static IP addresses for services using Gluetun network
# These must be within the gluetun_network subnet (172.20.0.0/16)
# Don't change unless you know what you're doing

RADARR_IPV4=172.20.0.10
SONARR_IPV4=172.20.0.11

# ============================================================================
# WIZARR CONFIGURATION
# ============================================================================
# The public URL where Wizarr (invite system) will be accessible
# Examples:
#   - Local: http://192.168.1.100:5690
#   - Domain: https://invite.yourdomain.com

WIZARR_URL=http://192.168.1.100:5690

# ============================================================================
# JELLYSTAT DATABASE CREDENTIALS
# ============================================================================
# Credentials for the PostgreSQL database used by Jellystat
# Change these to something secure!

JELLYSTATDB_USER=jellystat
JELLYSTATDB_PASS=your_secure_db_password_here

# ============================================================================
# JWT SECRET FOR JELLYSTAT
# ============================================================================
# Secret key for JWT token generation in Jellystat
# Generate a random string: openssl rand -base64 32
# Or use any long random string

JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING

# ============================================================================
# HOMARR CONFIGURATION
# ============================================================================
# Encryption key for Homarr dashboard
# Generate with: openssl rand -base64 32
# Or use any 32+ character random string

HOMARR_KEY=CHANGE_THIS_TO_RANDOM_STRING

# ============================================================================
# ADVANCED CONFIGURATION (OPTIONAL)
# ============================================================================

# Docker Compose Project Name (optional)
# Uncomment to customize the project name
# Default: uses directory name
# COMPOSE_PROJECT_NAME=media-stack

# Docker Compose File (optional)
# Uncomment if you rename docker-compose.yml
# COMPOSE_FILE=docker-compose.yml

# ============================================================================
# NOTES & TIPS
# ============================================================================
#
# 1. INITIAL SETUP:
#    - Fill in VPN credentials FIRST (WIREGUARD_KEY, WIREGUARD_ADD, VPN_SERVER_CITIES)
#    - Set secure passwords for QBIT_PASS, JELLYSTATDB_PASS
#    - Generate random strings for JWT_SECRET and HOMARR_KEY
#    - Keep default values for PUID/PGID/TZ unless you need to change them
#
# 2. AFTER FIRST STARTUP:
#    - Get API keys from each *arr app (Settings -> General)
#    - Update SONARR_KEY, RADARR_KEY, LIDARR_KEY, READARR_KEY
#    - Restart affected services: docker compose up -d
#
# 3. SECURITY:
#    - Never share this .env file
#    - Use strong, unique passwords
#    - Keep your Mullvad account secure
#    - Regularly update containers: docker compose pull && docker compose up -d
#
# 4. PATHS:
#    - BASE_PATH: Must exist and have write permissions
#    - MEDIA_SHARE: Should be mounted before starting containers
#    - Verify ownership: sudo chown -R 1000:1000 /mnt/media /opt/docker
#
# 5. TESTING:
#    - Test VPN: docker exec qbittorrent curl https://am.i.mullvad.net/connected
#    - Should return: "You are connected to Mullvad"
#
# ============================================================================
